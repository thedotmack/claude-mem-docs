---
title: 'Quick Start'
description: 'One command. Ten seconds. Done.'
---

import { Tabs, Tab } from '/snippets/tabs.mdx'

## Install (10 seconds)

<Tabs>
  <Tab title="npm (recommended)">
    ```bash
    npm install -g claude-mem && claude-mem install
    ```
  </Tab>
  <Tab title="bun (faster)">
    ```bash
    bun install -g claude-mem && claude-mem install
    ```
  </Tab>
</Tabs>

## Restart Claude Code

Close and reopen Claude Code.

## Done! 🎉

Claude now remembers everything. Start coding and watch your context load automatically.

---

## That's literally it.

No configuration. No setup. No learning curve. It just works.

When you start Claude Code, you'll see:

```
🧠 Claude Memory System - Active
────────────────────────────────
📚 Loading relevant memories...
✨ Context ready. Start coding.
```

## What Just Happened?

- ✅ Hooks installed (captures your sessions)
- ✅ MCP server configured (manages memories)
- ✅ Smart Trash™ enabled (never lose anything)
- ✅ Semantic search tools ready (ask Claude to run them when you need deep recall)
- ✅ `/compact` compression active (optional `/clear` saving if you enabled it)

All automatic. All instant.

## Try It Now

1. Ask Claude something: "Remember I prefer TypeScript strict mode"
2. Type `/compact`
3. Start a new session
4. Watch your preference appear automatically

## Need Help?

```bash
claude-mem status    # Check everything's working
claude-mem logs      # See what's happening
claude-mem --help    # All commands
```

<Card title="Something Wrong?" icon="wrench" href="/guides/troubleshooting">
  99% of issues are fixed with: `claude-mem install --force`
</Card>

## Want More?

<CardGrid cols={2}>
  <Card title="🎯 Instant Features" icon="sparkles" href="/instant-features">
    See everything that's automatic
  </Card>
  <Card title="🔧 Advanced Setup" icon="cog" href="/advanced/installation">
    Platform-specific, source builds, customization
  </Card>
</CardGrid>

---

**Stop reading. Start coding. Claude remembers now.** 🧠
