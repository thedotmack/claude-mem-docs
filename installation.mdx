---
title: 'Installation'
description: 'The only installation guide you need. One command.'
---

import { Tabs, Tab } from '/snippets/tabs.mdx'

## Installation

<Tabs>
  <Tab title="npm">
    ```bash
    npm install -g claude-mem && claude-mem install
    ```
  </Tab>
  <Tab title="bun (faster)">
    ```bash
    bun install -g claude-mem && claude-mem install
    ```
  </Tab>
</Tabs>

**Restart Claude Code. Done.**

---

## Verify It Worked

```bash
claude-mem status
```

You see green checkmarks:
```
✅ Installation: Complete
✅ Hooks: Installed
✅ MCP Server: Configured
✅ Memories: Ready
```

## Complete

Installation is finished. Claude remembers everything.

## Common Questions

<AccordionGroup>
  <Accordion title="What just got installed?">
    - Hooks that capture your Claude sessions
    - MCP server for memory management
    - Local database for storing memories
    - Smart Trash for recovery
    - All in `~/.claude-mem/` (about 100MB)
  </Accordion>

  <Accordion title="Do I need to configure anything?">
    No. It works with zero configuration. Everything has sensible defaults.
  </Accordion>

  <Accordion title="What if something goes wrong?">
    ```bash
    claude-mem install --force
    ```
    This fixes 99% of issues.
  </Accordion>

  <Accordion title="How do I update?">
    ```bash
    npm update -g claude-mem
    claude-mem install --force
    ```
  </Accordion>

  <Accordion title="How do I uninstall?">
    ```bash
    claude-mem uninstall
    npm uninstall -g claude-mem
    ```
  </Accordion>
</AccordionGroup>

## Start Using It

<CardGrid cols={2}>
  <Card title="🎯 See What's Automatic" icon="sparkles" href="/instant-features">
    Everything that happens without you doing anything
  </Card>
  <Card title="🚀 Try It Now" icon="play" href="/quickstart#try-it-now">
    Quick test to see it working
  </Card>
</CardGrid>

## Advanced Installation

Need something specific?

<Card title="Advanced Installation Options" icon="cog" href="/advanced/installation">
  Platform-specific setup, building from source, custom directories, permission issues, Windows setup, Docker, and more.
</Card>

Most users won't need advanced options.

---

**Installation complete. Start coding.**